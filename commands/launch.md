---
description: Launch party member — release builds & signing prep, store listing, age rating, submission checklists, review-rejection response, launch-day rollout and rollback plan.
allowed-tools: Read, Grep, Glob, Bash, PowerShell, Write, Edit, AskUserQuestion, Task
---

# /fullparty:launch

Engage the **launch** skill (see `skills/launch/SKILL.md`). Respond in the user's language.

- Every step the owner must click in a store console is prepared as an exact copy-paste checklist; the final submit and go/no-go are theirs.
- Boot/payment beacons go in **before** submission; from submission on, all server changes are additive-only (old client frozen in review).
- Privacy/terms URLs from `/fullparty:legal`, listing visuals with art/ui, launch-day watch handed to `/fullparty:liveops`.
