# Fullparty

You've been pointed at the Fullparty repo. Read `AGENTS.md` in this directory and follow it — it is the setup and operating guide for AI coding agents (including Claude Code). The skills under `skills/` are your playbooks; the slash commands under `commands/` are optional wrappers around them.
